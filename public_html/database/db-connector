var mysql = require('mysql')

var pool = mysql.createPool({
  connectionLimit : 10,
  host            : 'classmysql.engr.oregonstate.edu',
  user            : 'cs340_robbinli',
  password        : '9368',
  database        : 'cs340_robbinli'
});

module.exports.pool = pool;

