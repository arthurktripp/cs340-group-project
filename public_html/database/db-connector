var mysql = require('mysql')

var pool = mysql.createPool({
  connectionLimit : 10,
  host            : 'classmysql.engr.oregonstate.edu',
  user            : 'cs340_trippa',
  password        : '1507',
  database        : 'cs340_trippa'
});

module.exports.pool = pool;

